(()=>{'use strict';const DB='coaching_tracker_v3';function sync(){let s={};try{s=JSON.parse(localStorage.getItem(DB)||'{}')}catch{}const done=(s.items||[]).filter(x=>x.status==='done');const clients=new Set(done.filter(x=>x.name).map(x=>String(x.name).trim().toLowerCase())).size;const n=document.getElementById('v15Clients');if(n)n.textContent=clients;const title=document.querySelector('#stats .head h2');if(title)title.textContent='ACC 進度';const old=document.getElementById('statsPeople');if(old&&old.parentElement){const label=old.parentElement.querySelector('span');if(label)label.textContent='已記錄 Coachee'}}sync();new MutationObserver(sync).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});})();
